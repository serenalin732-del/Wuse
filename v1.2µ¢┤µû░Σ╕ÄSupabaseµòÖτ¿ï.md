# 五色 App v1.2 更新说明

## 🎉 新功能

### 1. ☀️🌙 深色模式
- 设置 → 外观 → 主题模式
- 三选项：浅色 / 深色 / 跟随系统
- 夜间护眼，所有界面适配

### 2. 🔤 字体大小调节
- 设置 → 外观 → 字体大小
- 三档：小 / 中 / 大
- 全局生效

### 3. 🎨 五色自定义
- 设置 → 外观 → 🎨 自定义五色
- 每种颜色都能换成你喜欢的（点色块选色）
- 一键恢复默认

### 4. 🌅 凌晨折叠
- 默认折叠 0:00–5:00（点击展开）
- 如果那段时间填了内容，会自动展开
- 设置里可关闭此功能

### 5. 📅 按星期模板
- 周一、周二...周日 + 默认，共 8 套模板
- 点「存为模板」选当前星期专属，或默认
- 「载入日常模板」自动匹配当天星期
- 周五带看模板 vs 周日休息模板从此分开

### 6. 📌 调色板 Sticky
- 调色板永远跟着滚动悬浮在顶部
- 不用再翻回顶部

### 7. 📸 导出今日截图
- 设置 → 数据 → 📸 导出今日截图
- 勾选要包含哪些模块（金句/时间表/青蛙/Todo/B区C区/评估/番茄）
- 生成一张精美长图，自动下载到相册

### 8. ☁️ 云同步（Supabase）
- 所有设备实时同步！iPhone 改了 iPad 立即看到
- 完全免费、安全
- 详细配置教程见下方

---

# Supabase 云同步配置教程（15 分钟）

## 为什么要配置？

之前的版本数据存在浏览器本地，**每个设备/浏览器各自存一份**——iPhone Safari 涂的色在 iPad 上看不到。

配置 Supabase 后，所有设备的数据**实时同步到同一份**。

## 📋 配置步骤

### 1️⃣ 注册 Supabase 账号

打开 [supabase.com](https://supabase.com) → Sign Up

- 用 Google / GitHub / 邮箱注册都可
- 完全免费
- 不要信用卡

### 2️⃣ 创建项目

登录后：
- 点 **New Project**
- 选一个 Organization（个人用默认那个）
- 项目名：`wuse`（或任意英文）
- 数据库密码：**点击 Generate a password**，让它自动生成（你不用记，反正用不到）
- Region：选 **East US** 或 **Singapore**（最快）
- 点 **Create new project**

⏳ 等 2 分钟项目启动完成。

### 3️⃣ 创建数据表（最关键的一步）

项目就绪后：

1. 左侧菜单点 **SQL Editor**（一个 [⌘] 图标）
2. 看到一个代码框
3. 把下面这段 **整段** 复制粘贴进去：

```sql
CREATE TABLE wuse_days (
  user_id text NOT NULL,
  date text NOT NULL,
  data jsonb,
  updated_at timestamptz DEFAULT now(),
  PRIMARY KEY (user_id, date)
);

ALTER TABLE wuse_days ENABLE ROW LEVEL SECURITY;

CREATE POLICY "anyone" ON wuse_days FOR ALL USING (true);
```

4. 点右下角绿色 **Run** 按钮
5. 看到 `Success. No rows returned` 就是成功

### 4️⃣ 获取连接信息

1. 左侧菜单点 **Settings**（齿轮）
2. 点 **API**
3. 复制以下两项（注意是这两项，不是别的）：

   - **Project URL**：类似 `https://abcdefghij.supabase.co`
   - **Project API keys** → **anon public**：一长串以 `eyJ` 开头的字符串

⚠️ 注意：**anon public** 这个 key 是设计成给前端使用的公开 key，**安全可以暴露**。不要复制 service_role key！

### 5️⃣ 在 App 里配置

1. 打开你的「五色」App
2. 点 ⚙ 齿轮 → **配置 Supabase**
3. 三个输入框：
   - **Supabase URL**：粘贴上面的 Project URL
   - **Anon Key**：粘贴上面的 anon public key
   - **用户标识**：填一个你自己的标识，比如你的邮箱 `serenalin732@gmail.com`
     - ⚠️ 关键：在所有设备上**填同一个标识**，才能同步同一份数据
4. 点 **测试连接** → 看到「✅ 连接成功」
5. 点 **保存并启用**
6. 等几秒，App 会自动把本地数据上传到云端

### 6️⃣ 在其他设备配置

iPad / 电脑 / 其他手机上：
- 打开同一个网址 https://wuse-9u5.pages.dev
- 设置 → 配置 Supabase
- 填**同样的** URL + Key + 用户标识
- 点保存
- 见证奇迹：所有数据自动出现 ✨

## 🔧 同步机制

- **保存即同步**：你每次改动（涂色、加待办、青蛙等），都会自动同步到云端
- **打开即拉取**：每次打开 App，会自动从云端拉最新数据
- **手动同步**：设置 → 立即同步（一次完整双向同步）
- **离线降级**：网不好的时候自动用本地，恢复联网后下次保存时自动上传

## ❓ 常见问题

**Q: anon key 会被别人盗用扣钱吗？**
A: 不会。anon key 是公开的，但配合"Row Level Security"+ user_id 隔离，别人不知道你的 user_id 也访问不了你的数据。免费层 500MB 数据库容量，你这种用法可能用 10 年都用不完。

**Q: 我的 user_id 用什么好？**
A: 推荐用邮箱（serenalin732@gmail.com）。重点是好记 + 唯一。不要随便填 "user1" 这种太简单的，可能被别人猜到。

**Q: 数据安全吗？**
A: 
- 数据存在 Supabase 的 PostgreSQL 数据库，企业级
- HTTPS 加密传输
- Row Level Security 行级权限隔离
- 你可以随时在 Supabase 控制台**导出全部数据**或**删库跑路**
- 你的 API key 也只存在你自己的浏览器，不上传任何第三方

**Q: 想换个 user_id 怎么办？**
A: 设置里 → 配置 Supabase → 改 user_id → 保存。会同步那个新 ID 对应的数据（如果是新 ID，云端没数据，会上传本地的数据）。

**Q: 用了云同步后，本地数据还存吗？**
A: 存。本地是云端的镜像。即使云端挂了，本地还能用。

**Q: Supabase 国内访问慢吗？**
A: 看运气，大部分时候能用。如果发现慢，选 Singapore 区域会好一些。
