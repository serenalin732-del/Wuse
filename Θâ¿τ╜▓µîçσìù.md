# 五色 App 部署指南

完整流程：**部署网页（10分钟）→ 添加到 iPhone 主屏幕（30秒）→ 配置 AI（可选，5分钟）**

---

## 📦 文件包内容

```
wuse-deploy/
├── index.html       ← 主程序
├── manifest.json    ← PWA 配置（让它能装到主屏幕）
├── sw.js            ← Service Worker（支持离线打开）
├── icon-32.png      ← 浏览器小图标
├── icon-180.png     ← iPhone 主屏幕图标
├── icon-192.png     ← Android 图标
├── icon-512.png     ← 大图标
└── 部署指南.md      ← 本文件
```

把整个文件夹上传到 Cloudflare Pages 即可。

---

## 第一步：部署到 Cloudflare Pages（10 分钟）

### 为什么选 Cloudflare Pages？
- 免费、无广告、永久使用
- 全球 CDN，速度快
- 不需要会写代码
- 不要信用卡

### 详细步骤

**1. 注册 Cloudflare 账号**

打开 https://dash.cloudflare.com/sign-up
- 输入邮箱、设密码
- 验证邮箱
- 完成注册（不需要任何付款信息）

**2. 进入 Pages**

登录后：
- 左侧菜单点 **Workers & Pages**
- 点 **Create** 或 **创建应用程序**
- 选 **Pages** 标签页
- 点 **Upload assets**（上传资源）

**3. 创建项目**

- 项目名：输入 `wuse`（或你喜欢的英文名，不要中文）
- 点 **Create project**

**4. 上传文件**

- 看到一个虚线框「Drag and drop your site assets」
- 把整个 `wuse-deploy` 文件夹**拖进去**
  - 或者点框内的"select from computer"选择文件夹
- 上传完会看到 6 个文件全部列出（index.html、manifest.json、sw.js、4个图标）
- 点 **Deploy site**

**5. 等待部署**

- 30 秒左右
- 完成后页面显示绿色 ✅ 「Success!」
- 给你一个网址：`https://wuse-xxx.pages.dev`（xxx 是随机字符）
- **点 "Continue to project" 看详情，再点上方那个网址打开网站**

✅ **第一步完成！** 你现在有一个永久可访问的网址了。

---

## 第二步：添加到 iPhone 主屏幕（30 秒）

⚠️ **必须用 Safari 浏览器**（不能用 Chrome）

1. 用 iPhone Safari 打开你的网址（`https://wuse-xxx.pages.dev`）
2. 点底部中间的 **分享按钮**（向上箭头那个 ⤴️）
3. 向下滑，找到 **添加到主屏幕**（Add to Home Screen）
4. 点 **添加**
5. 回主屏幕，看到「五色」图标，**点开就是 App 体验**——
   - 全屏，没有浏览器顶栏底栏
   - 独立的图标
   - 像原生 App 一样可以从应用切换器里看到

### iPad 上一样的操作

Safari → 分享 → 添加到主屏幕

### 电脑上怎么用？

直接把网址加书签即可。Chrome 用户还可以点地址栏右边的 **「安装应用」** 图标，把它装成桌面 App。

---

## 第三步：配置 AI 评估功能（可选，5 分钟）

如果你不需要 AI 自动生成评估，跳过本步即可，App 其他功能都不受影响。

### 申请 Anthropic API Key

1. 打开 https://console.anthropic.com
2. 注册账号（用你的常用邮箱）
3. 登录后，左侧找 **API Keys**
4. 点 **Create Key**
5. 给 key 起个名字（比如 `wuse-app`）
6. **复制生成的 key**（格式：`sk-ant-api03-xxxxxxxxxx...`）
   - ⚠️ 这个 key 只显示一次！立即保存到密码管理器
7. 在 **Billing** 页面充值最少 $5（最低充值额度）
   - AI 每次生成评估大约花 $0.005（半美分）
   - $5 够用约 1000 次，等于每天用一次能用 3 年

### 在 App 里配置

1. 打开你的「五色」App
2. 填一些今日数据（涂色、青蛙、todo 都行）
3. 点评估区右上角的 **✨ AI 生成** 按钮
4. 弹窗里粘贴你的 API key（sk-ant-... 开头那一串）
5. Key 会安全地存在你**当前设备的浏览器本地**，不上传任何地方
6. 之后 AI 生成就一键完成了

### 安全说明

- API key 只存在你自己设备的 localStorage 里
- 调用 API 是从你的浏览器**直接**到 Anthropic 服务器，不经过任何第三方
- 不同设备需要分别配置（或者你也可以选择不配置，只在某一台主力设备上用）

---

## ⚠️ 关于多设备同步

**当前版本数据保存在本设备的浏览器本地**，所以：

| 情况 | 行为 |
|------|------|
| iPhone Safari 涂色 → iPhone Safari 看 | ✅ 看到 |
| iPhone Safari 涂色 → iPad Safari 看 | ❌ 看不到（各自本地） |
| iPhone Safari 涂色 → Mac Chrome 看 | ❌ 看不到 |
| 清空浏览器缓存 / 卸载 App | ⚠️ 数据丢失 |

**临时解决方案：**
- 主要在一个设备上使用（推荐手机）
- 定期点「**导出 JSON**」做备份，保存到 iCloud Drive

**想要真正跨设备同步？**
等用一段时间觉得这个 App 值得，告诉我，我帮你升级到 v2 版本——接入 Supabase 免费数据库，所有设备实时同步。1-2 小时搞定。

---

## 🔧 常见问题

**Q: 上传文件夹时找不到选项？**
A: Cloudflare Pages 拖拽上传整个文件夹是支持的。如果浏览器不支持，可以：① 把所有文件**直接选中**（不要包括外层文件夹）一次性拖入，或 ② 改用 Chrome / Edge 浏览器。

**Q: 部署后打开网页是空白？**
A: 确认上传的有 `index.html`（文件名必须小写）。如果没有，重新上传。

**Q: 添加到主屏幕后图标显示不出来？**
A: 清掉 Safari 缓存（设置 → Safari → 清除历史记录与网站数据），重新打开网址再添加。

**Q: 我想用自己的域名（比如 wuse.soldbyserenalin.com）？**
A: Cloudflare Pages → 你的项目 → Custom domains → Add domain，输入子域名，按提示配 DNS 即可。

**Q: 怎么更新这个 App？**
A: 如果以后我帮你改了功能，把新的 index.html 重新上传到 Cloudflare Pages（在 deployments 页面 → Upload new build），整个过程 30 秒。

**Q: 数据会丢吗？**
A: 浏览器 localStorage 不会自动清除，但有几种情况会丢：
- 你主动「清除网站数据」
- 卸载浏览器后重装
- 长时间不用，iOS 系统可能清理 PWA 缓存（罕见）

所以**强烈建议每周点一次"导出 JSON"备份**，保存到 iCloud 或邮箱。

---

## 🎉 完成！

走完这三步，你就拥有了：

- ✅ 永久可用的网址（不依赖任何对话）
- ✅ iPhone / iPad / 电脑都能打开
- ✅ 主屏幕图标，秒开
- ✅ 离线也能用
- ✅ AI 评估生成（如配置）

享受你的 App！🐸✨

---

如果部署中遇到任何问题，把报错截图发给我，我帮你解决。
